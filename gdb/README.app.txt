To keep things simple for simple applications, the main build system
supports a mechanism to dynamically add application and library .c
files and header include paths.

Set these environment variables:

UC_TOOLS_APP_DIR
UC_TOOLS_LIB_DIR




